import React from "react";

const Dashboard = () => (
  <div>
    <h1>User Dashboard</h1>
    <p>Profile, rentals, listings, transactions, chat, notifications.</p>
  </div>
);

export default Dashboard;
